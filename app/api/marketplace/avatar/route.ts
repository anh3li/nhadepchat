import { getD1, getFilesBucket } from '../../../../db';
import { apiUser } from '../../../../lib/server-auth';
import { jsonError } from '../../../../lib/marketplace';

export async function POST(request:Request){const current=await apiUser(request);if('error'in current)return current.error;const form=await request.formData(),file=form.get('avatar');if(!(file instanceof File))return jsonError('Chưa chọn ảnh.',422);const ext=(file.name.split('.').pop()||'').toLowerCase();if(ext!=='webp'||file.type!=='image/webp')return jsonError('Avatar phải được tối ưu sang WEBP trước khi tải lên.',422);if(file.size>512*1024)return jsonError('Avatar WEBP tối đa 512KB.',422);const key=`users/${current.session.user.id}/avatar/${crypto.randomUUID()}.webp`,db=getD1(),old=await db.prepare('SELECT avatar_key FROM user_profiles WHERE user_id=?').bind(current.session.user.id).first<{avatar_key:string}>();await getFilesBucket().put(key,file.stream(),{httpMetadata:{contentType:'image/webp'},customMetadata:{ownerId:current.session.user.id}});await db.prepare('UPDATE user_profiles SET avatar_key=?,updated_at=? WHERE user_id=?').bind(key,Date.now(),current.session.user.id).run();await db.prepare('UPDATE user SET image=?,updatedAt=? WHERE id=?').bind(`/api/profile-avatar/${current.session.user.id}`,Date.now(),current.session.user.id).run();if(old?.avatar_key)await getFilesBucket().delete(old.avatar_key);return Response.json({ok:true,url:`/api/profile-avatar/${current.session.user.id}`});}
