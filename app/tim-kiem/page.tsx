/* eslint-disable @typescript-eslint/no-explicit-any */
import type { Metadata } from 'next';
import { MarketplaceHeader } from '../../components/MarketplaceHeader';
import { PublicProductGrid } from '../../components/PublicProductGrid';
import { getD1 } from '../../db';
import { productSelect } from '../../lib/marketplace';
export const metadata:Metadata={title:'Tìm kiếm bản vẽ | Nhà Đẹp Chất',description:'Tìm hồ sơ kiến trúc, kết cấu, MEP và dự toán.'};export const dynamic='force-dynamic';
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const q=((await searchParams).q||'').trim().slice(0,100);let products:any[]=[];if(q){const db=getD1(),isFreeSearch=q.toLocaleLowerCase('vi-VN').includes('miễn phí');if(isFreeSearch){const rows=await db.prepare(`${productSelect()} WHERE p.status='approved' AND p.is_free=1 ORDER BY p.approved_at DESC LIMIT 40`).all();products=rows.results}else{const like=`%${q.replace(/[%_]/g,'\\$&')}%`;const rows=await db.prepare(`${productSelect()} WHERE p.status='approved' AND (p.title LIKE ? ESCAPE '\\' OR p.category LIKE ? ESCAPE '\\' OR p.building_type LIKE ? ESCAPE '\\' OR EXISTS(SELECT 1 FROM product_formats pf2 WHERE pf2.product_id=p.id AND pf2.format LIKE ? ESCAPE '\\')) ORDER BY p.approved_at DESC LIMIT 40`).bind(like,like,like,like).all();products=rows.results}}return <><MarketplaceHeader/><main className="subpage"><div className="page-title"><p className="eyebrow">TÌM KIẾM</p><h1>{q?`Kết quả cho “${q}”`:'Tìm bản vẽ'}</h1><p>{q?`${products.length} hồ sơ được tìm thấy.`:'Nhập từ khóa ở thanh tìm kiếm phía trên.'}</p></div><PublicProductGrid products={products}/></main></>}
